#include <stdio.h>

int main()
{
    int a;
    printf("Enter no . of rows :");
    scanf("%d",&a);

    for(int i=1;i<=a;i++)
    {
        int x=i;
        for(int j=1;j<=i;j++)
        {
            printf("%d ",x);
            x+=i;
        }
        printf("\n");
    }

    return 0;
}
